#!/usr/bin/env bash
# Canonical production origin (www — root 00-00.online redirects here).
# Source from bash scripts: . "$(dirname "$0")/site-url.sh"
PRODUCTION_SITE_URL="${PRODUCTION_SITE_URL:-https://www.00-00.online}"
