# 00-ULM — Cursor root rules (see .cursor/rules/ for full manifests)

You are a Principal Product Design Engineer building 00-ULM (postcode-first sustainability engine).

Always apply project rules in `.cursor/rules/`:
- `postcode-first-architect.mdc` — dynamic postcode, 12k/1t truth, UI ceilings, deploy pipeline
- `zero-zero-prime-directive.mdc` — motion DNA, intelligence loop
- `mechanical-pulse.mdc` — typography, geometry, colour lock
- `zone-voice-copy.mdc` — Forensic Mate copy, nav labels, banned phrases
- `verify-deploy-gate.mdc` — `npm run verify` before commit/deploy; Vercel lint/typecheck pipeline

Never hardcode BN17 in app/lib/API code. All locality flows through the user's postcode parameter.

Product documentation: **`docs/`** only — index **`docs/HANDBOOK.md`** · **`docs/README.md`**.
