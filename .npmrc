# eslint + typescript stay in dependencies for verify / CI.
# Intentionally no package.json scripts named "lint" or "typecheck" — Vercel native Deployment Checks
# bind to those names and run in parallel (often "internal error"); gates live in verify + buildCommand.
