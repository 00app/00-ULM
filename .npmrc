# Vercel Native Deployment Checks run `npm run lint` / `typecheck` in parallel with the build.
# Production NODE_ENV omits devDependencies unless include=dev — @types/* must be present for tsc.
include=dev
