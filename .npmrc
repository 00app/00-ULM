# Do NOT add package.json scripts named lint / typecheck — Vercel Native Deployment Checks
# auto-bind and run them in parallel with build (flakes "failed unexpectedly").
# Use lint:ci + typecheck:ci; buildCommand runs vercel-build-gate.mjs; GH Actions gate prod.
include=dev
