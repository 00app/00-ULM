# v1.8.1 Readiness Report — Solo Focus “Single Template, Dual Entry”

**Date:** 2026-03-20  
**Spec reference:** `PROJECT-SPECIFICATION.md` §18.5 / §18.5.2  
**Build:** `npm run build` — **PASS** (exit 0, typecheck + lint OK; one existing `no-img-element` warning in `app/loading.tsx`).

---

## 1. Solo Focus convergence (§18.5)

| Item | Status | Evidence |
|------|--------|----------|
| Shared template classes | **PASS** | `JourneyBentoCard` and `SoloFocusOverlay` both use `expanded-solo-focus`, `solo-focus-content-stack`, `solo-focus-loop` where applicable (journey path includes embedded question loop; tips use loop when `journeyId` + callbacks present). |
| Portal to `document.body` | **PASS** | `JourneyBentoCard` expanded path: `createPortal(..., document.body)`. `SoloFocusOverlay`: **`createPortal(..., document.body)` inside the component** (Zone only wraps `AnimatePresence`; overlay owns the portal). *Manual QA:* confirm tip close exit motion if you rely on Framer `exit` with a portaled subtree. |
| Close control | **PASS** | Solo Focus close is `BackArrowDownLeft` in `.solo-focus-close-circle` (40px circle) inside **`.solo-focus-expanded-toolbar`** with category (same row, vertically centered — not floating absolute). **Same SVG path** as Zone bento **`card-top-arrow`** / Trinity link arrow (**180°** in component). **Motion:** **`INTRO_FADE_UP_NO_DELAY`** + **`SPRING_TAP`**. No `×` glyph in Solo Focus paths. |

---

## 2. Data stack lockdown

| Item | Status | Evidence |
|------|--------|----------|
| Label↔value gap | **PASS** | `.data-stack` / `.data-stack--tight` / `.solo-focus-data-stack` use **`gap: 0`** (unified data lock); SAVE/CARBON columns use `solo-focus-data-stack` + `data-stack` in both entry components. |
| No label overlap | **PASS** | SAVE/CARBON use flex column stacks; `.data-label` has no `position: absolute` in Solo Focus impact rules. Close sits in the **header toolbar** flex row (not absolute in the expanded template). |

---

## 3. Zai re-wiring (JSONB-first)

| Item | Status | Evidence |
|------|--------|----------|
| JSONB over normalized | **PASS** | Logged-in path: `mergeJourneyAnswerMaps(normalizedAnswers, jsonbAnswers)` — overlay keys win per journey. Body answers merge last on top of DB snapshot. |
| Mandatory opener | **PASS** | `mandatoryOpenerPrefix()` drives system instruction; post-process lowercases reply and prepends prefix if missing. Copy aligned to “since you have {heating}…” / “since you're in {postcode}…” (lowercase brand voice after `text.toLowerCase()`). |

---

## 4. Home footnote

| Item | Status | Evidence |
|------|--------|----------|
| `SoloFocusPriceCapFooter` | **PASS** | Rendered only when `journeyId === 'home'` in `JourneyBentoCard` and `SoloFocusOverlay`. Styled at **10pt** Roboto in `.solo-focus-price-cap-footnote`; footer uses `margin-top: auto` inside flex column `.expanded-solo-focus` for bottom-of-stack placement. |

---

## 5. Purge & validation

| Item | Status | Evidence |
|------|--------|----------|
| `app/expand/` | **PASS** | No `expand` app routes in tree; glob finds no `app/expand/**/*.tsx`. |
| `ROUTES.EXPAND` | **PASS** | Absent from `lib/routes.ts`. |
| Production build | **PASS** | `npm run build` completed; static generation **27/27** (Next 14 route table lists 11 prerendered `○` app routes plus `_not-found` / health; total prerendered units per build log = 27). No hydration errors reported at build time. |

---

## Conclusion

**Dual entry is aligned to the §18.5.2 template:** journeys open via `JourneyBentoCard` → portaled Solo Focus shell; tips/filler open via `SoloFocusOverlay` → same shell, same CSS hooks, same **bento-arrow** close stroke (**`BackArrowDownLeft`**, shared motion presets), same data-stack spacing contract, same Home-only price-cap footer. Zai context merges **JSONB over normalized** with **mandatory opener** enforcement.

**Note:** Static path count in CI/docs may say “28” vs current **27** prerender jobs — reconcile against your deployment checklist if a specific page was removed or marked dynamic.
