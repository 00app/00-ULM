---
description: Universal Project Manifest for Zero Zero (00-00)
globs:
  - "**/*"
alwaysApply: true
---

# Zero Zero (00-00) Prime Directive

## 1. Motion DNA (`lib/motion-family.ts` + `lib/animations.ts`)

**Family Liquid + Atomic Assembly:** `FAMILY_EASE` `[0.22, 1, 0.36, 1]` — **0.8s** chapter, **1.0s** crystallize (`FAMILY_DUR_ATOMIC`), **0.4s** interaction. Read-time: **200ms/word** after lock (`atomicWordHoldMs`). See `docs/MOTION-FAMILY.md`.

| Surface | Style | Rule |
| --- | --- | --- |
| **`/` + `/intro` (logo route)** | **Atomic power-on** | `AtomicLogo` + atomic kinetic words (`opacityTicker`); legacy `GlitchLogo` retained for reference only. |
| **`/profile/summary`** | **Atomic Assembly ticker** | `IntroWordCycle` + **`opacityTicker`**: blur cloud → sharp word (`familyAtomicAssembly`); dwell via `atomicWordHoldMs`. |
| **`/profile` questions** | **Centered atomic cross-fade** | `familyProfileStepProps` = `familyAtomicAssembly`; `AnimatePresence mode="wait"`. |
| **`/likes`, `/settings`, `/zai`** | **Atomic enter** | `familyPageEnterProps` / message bloom; likes pulse unchanged. |
| **Zone grid** | **Atomic crystallize + morph** | `ZONE_ATOMIC_BENTO_VARIANTS`; expand `layoutId` + `FAMILY_LAYOUT_SPRING`. |
| **Solo Focus** | **Liquid morph shell** | `ExpandedCardShell` + `layoutId`; embedded Q zip-shut industrial. |

**Springs:** `KINETIC_SPRING` + `LAYOUT_SPRING` for Zone layout morph (Wave B). Do not purge boot/glitch CSS keyframes.

## 2. Intelligence Loop (Handbook Alignment)

- **Trigger:** Oracle VPS (Hermes) or Vercel Cron hits **`GET` or `POST`** `/api/cron/zone-research` **weekly Monday 05:00** (`0 5 * * 1`, Bearer **`CRON_SECRET`**). Per-category JIT scrape still fires on Tip +1 answer.
- **Birth (Zone discovery cards from the embedded journey):** **`POST /api/answers`** → discovery race → `discovery.new_card_data` / `grid_pulse_card` → **`injectNewDiscoveryCard`**. This is the **canonical** birth path for MC answers in Solo Focus / bento.
- **Supplemental (capped):** **`POST /api/zone/injections`** (trap follow-up) and **`POST /api/research/question-card`** (free-form Ask) — not the primary answer-loop birth; all respect **`MAX_DISCOVERY_INJECTIONS_PER_JOURNEY`**.
- **Neon:** Persisted invoke metadata column is **`research_snapshot`** (JSONB).
- **ULM ceilings:** 24 bento cells, 3 discovery injects/journey, Rock rail 6→12 — `lib/zone/ulmLimits.ts`, `docs/ULM-APPLICATION-LOOP.md`.
- **Reasoning:** Gemini emits **`agent_headline`** (~20 words) + **`architect_prose`** as exactly three paragraphs (what / why / how embedded in prose — no structural labels in the text).
- **Persona:** Zai Senior Auditor (industrial, direct, zero-fluff; lowercase where natural).

## 3. UI Constraints

- **Summary:** HELLO → name → locality (scale / wrap for long single-word tokens) — ticker words from `buildSummaryStaccatoWords` + `IntroWordCycle`.
- **Clean Focus:** Always run **`stripExpandedCardTitleNoise`** on Solo Focus H1s.
- **Neon host:** Pooler hostname in **`DATABASE_URL`** must match **`MANIFEST_NEON_POOLER_HOST`** in `lib/intelligence/manifest.ts` (password only via Neon / Vercel env).

Reference: `docs/HANDBOOK.md`, `docs/INTELLIGENCE-LOOP-MANIFEST.md`, and `mechanical-pulse.mdc`.
