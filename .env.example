# Neon PostgreSQL — get your connection string from https://console.neon.tech
# Copy this file to .env.local and set values for local dev.
# For Vercel: Project → Settings → Environment Variables.

DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Gemini (Ask Zai / 001 Scraper bridge). Get key from https://aistudio.google.com/apikey
# Never commit the real key. Use .env.local and Vercel env vars only.
GEMINI_API_KEY=

# Optional: require x-scraper-key header on POST /api/scrape-sync (set same value in 001 Crawler)
# SCRAPER_SECRET=
